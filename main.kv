MDBoxLayout:
    orientation: 'vertical'
    md_bg_color: app.theme_cls.bg_light

    MDToolbar:
        title: 'BOM update'

    MDBoxLayout:
        padding: ['50dp', 0]
        orientation: 'vertical'

        MDGridLayout:
            cols: 2

            MDLabel:
                id: lab_old
                halign: 'center'
                text: 'Select the old bom list:'
                # md_bg_color: app.theme_cls.primary_color

            MDRelativeLayout:
                MDRoundFlatIconButton:
                    text: 'Browse'
                    icon: 'folder'
                    pos_hint: {'center_x': .3, 'center_y': .5}
                    on_press: app.set_bom_old_path()

        MDLabel:
            id: lab_old_path
            halign: 'center'
            size_hint_y: .5
            text: ''

        MDSeparator

        MDGridLayout:
            cols: 2

            MDLabel:
                id: lab_new
                halign: 'center'
                text: 'Select the new bom list:'

            MDRelativeLayout:
                MDRoundFlatIconButton:
                    text: 'Browse'
                    icon: 'folder'
                    pos_hint: {'center_x': .3, 'center_y': .5}
                    on_press: app.set_bom_new_path()

        MDLabel:
            id: lab_new_path
            halign: 'center'
            size_hint_y: .5
            text: ''

        MDSeparator

        MDRelativeLayout:
            MDFillRoundFlatButton:
                id: run_button
                text: 'Run!'
                font_size: 20
                pos_hint: {'center_x': .5, 'center_y': .5}
                size_hint: .8, .8
                on_press: app.update_bom()




